[Env setup:]
1. install mocha in global:
	npm install -g mocha
  Note: if you want run nodeunit, also install nodeunit in global.

2. install chai and sinon in local project:
	npm install chai, sinon
